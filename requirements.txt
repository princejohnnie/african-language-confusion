# Core data handling
pandas==3.0.3
requests==2.34.2

# Dataset loaders (Aya, Dolly, PolyWrite) -- Build_Prompt_Dataset.ipynb
datasets==5.0.0
huggingface_hub==1.24.0

# English-only filter for the ShareGPT crosslingual base pool -- Build_Prompt_Dataset.ipynb
langdetect==1.0.9

# Open-source models via OpenRouter -- Open_Source_Models.ipynb
openai==2.46.0

# Closed-source models via CMU AI Gateway -- Closed_Source_Models.ipynb
langchain-openai==1.3.5
langchain-core==1.5.0

# .env API key loading -- model_clients.py
python-dotenv==1.2.2

# GlotLID language ID for LPR/WPR metrics -- compute_metrics.py, Compute_Metrics.ipynb.
# fasttext-wheel is used instead of the official fasttext package, which fails to build
# on Windows/MSVC (a pybind11 incompatibility); fasttext-wheel ships a prebuilt wheel.
fasttext-wheel==0.9.2

# Run the notebooks themselves
# jupyter==1.1.1
# ipykernel==7.3.0
